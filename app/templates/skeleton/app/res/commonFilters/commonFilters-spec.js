describe('commonFilters', function() {

    beforeEach(module('cafyneapp'));

    //it('should ...', inject(function($filter) {
    //
    //    var filter = $filter('commonFilters');
    //
    //    expect(filter('input')).toEqual('output');
    //
    //}));

});
