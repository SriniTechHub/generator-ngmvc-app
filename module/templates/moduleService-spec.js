describe('<%= _.camelize(name) %>Service', function() {

    beforeEach(module('<%= _.camelize(name) %>'));

    it('should ...', inject(function(<%= _.camelize(name) %>Service) {

        //expect(<%= _.camelize(name) %>.doSomething()).toEqual('something');

    }));

});
